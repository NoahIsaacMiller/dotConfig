local M = {} --

function M.apply(config)
    config.colors = {
        -- cursor_bg = '#22c55e',  -- 光标颜色（绿色）
        -- cursor_fg = '#000000',  -- 光标内文字颜色（黑色，与背景对比）
        cursor_fg = '#ffffff'
    }
end

return M
